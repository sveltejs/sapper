import {
  ComponentConstructor,
  ComponentModule,
} from './shared';

export interface ComponentLoader {
  js: () => Promise<ComponentModule>,
  css: string[]
};

export interface Route {
  pattern: RegExp;
  parts: Array<{
    i: number;
    params?: (match: RegExpExecArray) => Record<string, string>;
  }>;
};

export const ErrorComponent: ComponentConstructor
export const components: ComponentLoader[]
export const ignore: RegExp[]
export const root_comp: { preload: (props: { params: Params, query: Query }) => Promise<any>; }
export const routes: Route[]
